---
- name: deploy nginx
  hosts: cluster
  become: true

  tasks:
    - name: Create nginx deployment
      shell: 
          su ubuntu
          kubectl apply -f /home/ubuntu/ansible-inventory/Deployment.yaml
    - name: Create service
      shell: 
          su ubuntu
          kubectl apply -f /home/ubuntu/ansible-inventory/Service.yaml